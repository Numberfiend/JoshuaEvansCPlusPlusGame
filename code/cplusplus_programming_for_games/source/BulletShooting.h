//#pragma once

//sdl header files
//#include <SDL.h>
//#include <SDL_image.h>
//vector for managing array of bullets (Similar to arraylist in c#)
//#include <vector>
//#include <math.h>
//#include <algorithm>
//#include "Player.h"

//#define PI 3.14159265

/*using namespace std;

struct Bullet {
	float x, y, rotation, distance;
};

class BulletShooting {
public:
	BulletShooting(SDL_Renderer* renderer, Player* player) : renderer(renderer), player(player) {}

	void init();

	void processInput(bool* keyDown);

	void update();

	void render();

	void clean();
private:
	SDL_Renderer* renderer;
	SDL_Texture* bulletTexture;
	vector<Bullet> bullets;

	Player* player;


	const int SHOOT_TIMER_MS = 300;

	const int BULLET_VELOCITY = 15;
	unsigned int lastShot = 0;
};*/